coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.25%
    patch:
      default:
        target: 90%
        threshold: 0%

codecov:
  notify:
    after_n_builds: 2
