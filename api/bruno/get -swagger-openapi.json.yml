info:
  name: get /swagger/openapi.json
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/swagger/openapi.json"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/swagger/openapi.json"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
