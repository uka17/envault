info:
  name: /login
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}:{{port}}/api/{{version}}/users/login"
  body:
    type: json
    data: |-
      {
          "email": "test@test.ru",
          "password": "Password8"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
