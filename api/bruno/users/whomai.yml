info:
  name: /whomai
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}:{{port}}/api/{{version}}/users/whoami"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
