info:
  name: /users/me
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}:{{port}}/api/{{version}}/users/me/password"
  body:
    type: json
    data: |-
      {
        "currentPassword": "Password8",
        "newPassword": "Password9"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
