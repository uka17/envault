info:
  name: Change user password
  type: http
  seq: 6
  tags:
    - User

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/users/me/password"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Password changed successfully
    request:
      url: "{{baseUrl}}/api/v1/users/me/password"
      method: PATCH
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
  - name: 401 Response
    description: Missing or invalid JWT token, or its session is revoked or expired
    request:
      url: "{{baseUrl}}/api/v1/users/me/password"
      method: PATCH
    response:
      status: 401
      statusText: Unauthorized
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "code": "incorrect_token"
          }
  - name: 422 Response
    description: Validation error or incorrect current password
    request:
      url: "{{baseUrl}}/api/v1/users/me/password"
      method: PATCH
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "errors": [
              {
                "field": "email",
                "message": "must be a valid email"
              }
            ]
          }
  - name: 422 Response
    description: Validation error or incorrect current password
    request:
      url: "{{baseUrl}}/api/v1/users/me/password"
      method: PATCH
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/xml
      body:
        type: xml
        data: |-
          <?xml version="1.0" encoding="UTF-8"?>
          <root>
            <errors>[object Object]</errors>
          </root>

docs: Changes the password and revokes all sessions, including the current one. The next protected request returns 401 and the user has to log in again.
