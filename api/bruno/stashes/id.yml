info:
  name: /:id
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}:{{port}}/api/{{version}}/stashes/:id"
  params:
    - name: id
      value: "11"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
