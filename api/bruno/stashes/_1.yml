info:
  name: /_1
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}:{{port}}/api/{{version}}/stashes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
